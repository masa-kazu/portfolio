<div class="main">
  <div class="card devise-card">
    <div class="form-wrap">
      <div class="form-group text-center">
        <h1>サインイン</h1>
        <h2 class="mx-auto">BestBefore</h2>
        <p class="text-secondary">賞味期限をチェックしよう</p>
      </div>
      <%= form_with scope: resource, as: resource_name, url: session_path(resource_name), local: true do |f| %>
        <%= devise_error_messages! %>
        <div class="form-group">
          <%= f.email_field :email, autofocus: true, placeholder: "メールアドレス", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.password_field :password, autocomplete: "off", placeholder: "パスワード", class: "form-control" %>
        </div>

        <div class="actions">
          <%= f.submit "サインインする", class: "btn btn-primary w-100 border border-white",style: "background-color: #cd853f;" %>
        </div>
      <% end %>

      <br>

      <p class="devise-link">
        アカウントをお持ちでないですか？
        <%= link_to "登録する", new_user_registration_path, class: "devise-link" %>
      </p>

      <p class="devise-link">
        ゲストログインの場合にはコチラ。
        <%= link_to 'ゲストログイン（閲覧用）', users_guest_sign_in_path, class: "devise-link", method: :post %>
      </p>

    </div>
  </div>
</div>